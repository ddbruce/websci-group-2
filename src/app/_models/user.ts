// User class definition, derived from Mongo backend schema

export class User {
    id: number;
    firstName: string;
    lastName: string;
    company: string;
    username: string;
    password: string;
}
